version: "1.0"

services:
  flask:
    build: .
    container_name: tripedia-flask
    environment:
      MONGO_URI: mongodb://mongo:27017/Tripedia
    ports:
      - "6666:6666"
    networks:
      - mongoDB
    restart: unless-stopped

networks:
  mongoDB:
    external: true
